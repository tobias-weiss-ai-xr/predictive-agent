"""Pytest configuration for predictive-agent tests."""
import sys
import os

# Add project root to path so we can import predictive_agent package
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
